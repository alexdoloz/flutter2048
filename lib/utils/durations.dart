/// Constants with durations
class Durations {
  /// Duration of scaling the tile when it doubles
  static const tileScaleAnimationDuration = Duration(milliseconds: 250);

  /// Duration of changing opacity
  static const tileOpacityAnimationDuration = Duration(milliseconds: 300);

  /// Delay between making a move and creating new tile
  static const newTileDelay = Duration(milliseconds: 300);

  /// Duration of tile movement
  static const tileMovementDuration = Duration(milliseconds: 250);
}